apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taskmanager-backend
  namespace: argocd
  annotations:
    # ArgoCD Image Updater annotations for automatic image updates
    argocd-image-updater.argoproj.io/image-list: backend=608713827966.dkr.ecr.us-east-1.amazonaws.com/taskmanager-app
    argocd-image-updater.argoproj.io/backend.update-strategy: latest
    argocd-image-updater.argoproj.io/backend.allow-tags: regexp:^backend-[0-9]+$
    argocd-image-updater.argoproj.io/backend.image-name: backend-latest
    argocd-image-updater.argoproj.io/write-back-method: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/BIGRS-ITI/Platform.git
    targetRevision: main
    path: apps/Backend
  
  destination:
    server: https://kubernetes.default.svc
    namespace: taskmanager
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
