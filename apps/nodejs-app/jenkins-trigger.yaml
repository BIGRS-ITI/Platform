apiVersion: batch/v1
kind: Job
metadata:
  name: trigger-jenkins-pipeline
  namespace: jenkins
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: trigger
        image: curlimages/curl:7.88.1
        env:
          - name: JENKINS_TOKEN
            valueFrom:
              secretKeyRef:
                name: jenkins-token
                key: jenkins_token
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "Waiting for Jenkins..."
            until curl -s http://jenkins.jenkins.svc.cluster.local:8080/login > /dev/null; do
              sleep 5
            done

            echo "Jenkins ready, fetching crumb..."
            CRUMB=$(curl -s -u admin:$JENKINS_TOKEN \
              http://jenkins.jenkins.svc.cluster.local:8080/crumbIssuer/api/json \
              | jq -r '.crumb')

            echo "Triggering pipeline..."
            curl -X POST \
              http://jenkins.jenkins.svc.cluster.local:8080/job/codebuild/build \
              -u admin:$JENKINS_TOKEN \
              -H "Jenkins-Crumb: $CRUMB"

              echo "Triggered successfully!"

